#ifndef HATARAKI_BASICMOTION_TIMER
#define HATARAKI_BASICMOTION_TIMER

class Timer
{
private:		
	struct timespec start;

public:

	Timer()
	{   
		countdownTime = 0;
	}

	void start() {
		clock_gettime(CLOCK_REALTIME, &now);
	}

	double seconds()
	{		
		struct timespec now;
		clock_gettime(CLOCK_REALTIME, &now);

		long nsDiff = now.tv_nsec - last.tv_nsec;


	}

	static double deltaSeconds(struct timespec & t0, struct timespec & t1) {




	}

};

#endif